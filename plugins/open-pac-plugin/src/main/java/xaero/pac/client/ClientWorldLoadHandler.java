package xaero.pac.client;

import net.minecraft.client.multiplayer.ClientLevel;
import net.minecraft.client.player.LocalPlayer;

import javax.annotation.Nullable;

public class ClientWorldLoadHandler {

	public void handle(ClientLevel world, @Nullable LocalPlayer player) {
		//ClientWorldMainCapability mainCap = (ClientWorldMainCapability) world.getCapability(ClientWorldCapabilityProvider.MAIN_CAP).orElse(null);
	}

}
